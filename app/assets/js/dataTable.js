$(document).ready(function () {
  // table for agent dashboard
  var table1 = $("#dataTable1").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
  });

  // table for agent editing
  var table2 = $("#dataTable2").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
  });

  // table for agent for validation
  var table3 = $("#dataTable3").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
  });

  // table for agent validated
  var table4 = $("#dataTable4").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
  });

  
  // table for voo/om dashboard
  var table5 = $("#dataTable5").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
  });
  
  // table for voo/om for validation
  var table6 = $("#dataTable6").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
  });

  // table for voo/om validated
  var table7 = $("#dataTable7").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
  });

  // table for acctg dashboard
  var table8 = $("#dataTable8").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
    
  });

  // table for acctg for validation
  var table9 = $("#dataTable9").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
  });
  var table10 = $("#dataTable10").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
  }); 


  // table for acct validated
  var tableUsers = $("#tableUsers").DataTable({
    paging: true,
    searching: true,
    pageLength: 10,
  });  

  
  
  
});